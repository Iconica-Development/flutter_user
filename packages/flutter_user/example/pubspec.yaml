name: example
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  flutter_user:
    path: ../../flutter_user

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
